name: syntax_highlighter
description: A Dart syntax highlighter for showing code in a Flutter application.
version: 0.2.0
homepage: https://github.com/viztushar/syntax_highlighter

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  string_scanner: ^1.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
